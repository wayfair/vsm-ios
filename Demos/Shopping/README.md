# Shopping demo

The sample app lives under **Swift 6** (`AsyncStream`, actors, `StateSequence`). Open:

`Swift 6/Shopping.xcodeproj`

The older Swift 5 / Combine-based duplicate was removed to align demos with VSM 2.0 and avoid publisher bridging in sample code.
